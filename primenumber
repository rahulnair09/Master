n=int(input("Enter a number to check whether a number is prime or not:"))
for i in range(2,n):
	if n % i==0:
		print("{0} is not prime number"
		.format(n))
		break
	else:
		print("{0} is prime number".format(n))
		break